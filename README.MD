# Student Performance Prediction (Simple ML Project)

This is a machine learning project that predicts a student's marks from simple study-related features. I built this to learn the whole ML workflow: load data → train a model → evaluate → get predictions.

---

## 📁 Files in this repository

- **`main.py`** — Runs the program: loads data, trains the linear regression model, shows predictions.
- **`model.py`** — Contains the model training and prediction functions.
- **`student_performance.csv`** — Dataset with 100 rows (Hours_Studied, Attendance, Sleep_Hours, Previous_Score, Marks).
- **`requirements.txt`** — Required Python packages.

---

## ▶️ How to run (Windows / VS Code)

1. Open this folder in VS Code  
2. Open the terminal (View → Terminal)  
3. Install dependencies:

```bash
pip install -r requirements.txt
